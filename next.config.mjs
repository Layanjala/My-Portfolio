const nextConfig = {
	output: "export",
	basePath: "/My-Portfolio",
	assetPrefix: "/My-Portfolio/",
	images: {
		unoptimized: true,
	},
	typescript: {
		ignoreBuildErrors: true,
	},
};

export default nextConfig;
